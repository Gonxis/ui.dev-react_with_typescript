### (Project) API

You can find the starting place for this video here: [API Start](https://github.com/uidotdev/react-with-typescript-course/tree/2-TypeScript-Config)

The Github commit for the completed code can be found here: [Github Commit](https://github.com/uidotdev/react-with-typescript-course/commit/131c4ea98c255bcfe5b6256ceb6c18e5bbdb0070)


---------------------------------------------------------------------------------

Comments:

* Hi , I would like to ask why "message" was not annotated to be part of the User Type since we would access repos.message in the getRepos function

```ts
type User = {message:string, followers: number}
async function getRepos(username: string) :Promise<User[]>{
  const res = await fetch(
    `https://api.github.com/users/${username}/repos${params}&per_page=100`
  );
  const repos = await res.json();
  if (repos.message) {
    throw new Error(getErrorMsg(repos.message, username));
  }
  return repos;
}
```
    * First I also wondered about that. But I think it isn't necessary, as it's only in case of an error relevant. And in case of error an Error will be thrown anyway.
    
    * Oh , so that means we dont need to access it (repos.message) at all. Because ideally typescript should throw compile time errors since the message type is not defined. I dont know why typescript is not throwing the error ??
    
    * Fetch only raises an error if the request never resolves. If the response has any headers then fetch returns OK and passes the result along for handling. [docs](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch) - the first bullet in the intro, specifically
    We check for the `message` property because that is how github responds with an error (typically 404 user not found, but I guess there are other potential errors such as rate limiting, server fault, etc).
    
    * getRepos returns a list of Repos, not users.
